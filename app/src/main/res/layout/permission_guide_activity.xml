<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_permission_guide_gradient"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- 顶部图标 -->
    <ImageView
        android:id="@+id/iv_permission_icon"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@drawable/ic_permission_storage"
        android:contentDescription="@string/permission_request_title" />

    <!-- 标题 -->
    <TextView
        android:id="@+id/tv_permission_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/permission_guide_title"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- 说明文字 -->
    <TextView
        android:id="@+id/tv_permission_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:text="@string/permission_guide_desc"
        android:textColor="#E3F2FD"
        android:textSize="14sp" />

    <!-- 隐私协议和免责声明 -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_privacy_policy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:text="@string/permission_guide_privacy"
            android:textColor="#FFEB3B"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=" | "
            android:textColor="#BBDEFB"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/tv_disclaimer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="8dp"
            android:text="@string/permission_guide_disclaimer"
            android:textColor="#FFEB3B"
            android:textSize="14sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- 底部按钮区域 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:orientation="vertical">

        <!-- 同意并继续按钮 -->
        <Button
            android:id="@+id/btn_agree"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@drawable/bg_permission_btn_agree"
            android:text="@string/permission_btn_agree"
            android:textColor="#2196F3"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- 退出应用按钮 -->
        <Button
            android:id="@+id/btn_exit"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="8dp"
            android:background="@android:color/transparent"
            android:text="@string/permission_btn_exit"
            android:textColor="#90CAF9"
            android:textSize="14sp" />

    </LinearLayout>

</LinearLayout>
