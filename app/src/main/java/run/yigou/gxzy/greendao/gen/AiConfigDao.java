package run.yigou.gxzy.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import run.yigou.gxzy.greendao.entity.AiConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "AI_CONFIG".
*/
public class AiConfigDao extends AbstractDao<AiConfig, String> {

    public static final String TABLENAME = "AI_CONFIG";

    /**
     * Properties of entity AiConfig.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property AiConfigId = new Property(0, String.class, "AiConfigId", true, "AI_CONFIG_ID");
        public final static Property ProvideAi = new Property(1, String.class, "ProvideAi", false, "PROVIDE_AI");
        public final static Property ApiKey = new Property(2, String.class, "ApiKey", false, "API_KEY");
        public final static Property AiUrl = new Property(3, String.class, "AiUrl", false, "AI_URL");
    }

    private DaoSession daoSession;


    public AiConfigDao(DaoConfig config) {
        super(config);
    }
    
    public AiConfigDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"AI_CONFIG\" (" + //
                "\"AI_CONFIG_ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: AiConfigId
                "\"PROVIDE_AI\" TEXT," + // 1: ProvideAi
                "\"API_KEY\" TEXT," + // 2: ApiKey
                "\"AI_URL\" TEXT);"); // 3: AiUrl
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"AI_CONFIG\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AiConfig entity) {
        stmt.clearBindings();
 
        String AiConfigId = entity.getAiConfigId();
        if (AiConfigId != null) {
            stmt.bindString(1, AiConfigId);
        }
 
        String ProvideAi = entity.getProvideAi();
        if (ProvideAi != null) {
            stmt.bindString(2, ProvideAi);
        }
 
        String ApiKey = entity.getApiKey();
        if (ApiKey != null) {
            stmt.bindString(3, ApiKey);
        }
 
        String AiUrl = entity.getAiUrl();
        if (AiUrl != null) {
            stmt.bindString(4, AiUrl);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AiConfig entity) {
        stmt.clearBindings();
 
        String AiConfigId = entity.getAiConfigId();
        if (AiConfigId != null) {
            stmt.bindString(1, AiConfigId);
        }
 
        String ProvideAi = entity.getProvideAi();
        if (ProvideAi != null) {
            stmt.bindString(2, ProvideAi);
        }
 
        String ApiKey = entity.getApiKey();
        if (ApiKey != null) {
            stmt.bindString(3, ApiKey);
        }
 
        String AiUrl = entity.getAiUrl();
        if (AiUrl != null) {
            stmt.bindString(4, AiUrl);
        }
    }

    @Override
    protected final void attachEntity(AiConfig entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public AiConfig readEntity(Cursor cursor, int offset) {
        AiConfig entity = new AiConfig( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // AiConfigId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // ProvideAi
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // ApiKey
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // AiUrl
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AiConfig entity, int offset) {
        entity.setAiConfigId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setProvideAi(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setApiKey(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setAiUrl(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final String updateKeyAfterInsert(AiConfig entity, long rowId) {
        return entity.getAiConfigId();
    }
    
    @Override
    public String getKey(AiConfig entity) {
        if(entity != null) {
            return entity.getAiConfigId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AiConfig entity) {
        return entity.getAiConfigId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
