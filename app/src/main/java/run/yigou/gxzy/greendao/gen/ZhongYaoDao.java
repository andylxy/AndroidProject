package run.yigou.gxzy.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import run.yigou.gxzy.greendao.entity.ZhongYao;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ZHONG_YAO".
*/
public class ZhongYaoDao extends AbstractDao<ZhongYao, Long> {

    public static final String TABLENAME = "ZHONG_YAO";

    /**
     * Properties of entity ZhongYao.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property YaoId = new Property(0, Long.class, "yaoId", true, "_id");
        public final static Property ID = new Property(1, int.class, "ID", false, "ID");
        public final static Property YaoList = new Property(2, String.class, "yaoList", false, "YAO_LIST");
        public final static Property Name = new Property(3, String.class, "name", false, "NAME");
        public final static Property Height = new Property(4, int.class, "height", false, "HEIGHT");
        public final static Property Text = new Property(5, String.class, "text", false, "TEXT");
        public final static Property SignatureId = new Property(6, long.class, "signatureId", false, "SIGNATURE_ID");
        public final static Property Signature = new Property(7, String.class, "signature", false, "SIGNATURE");
    }


    public ZhongYaoDao(DaoConfig config) {
        super(config);
    }
    
    public ZhongYaoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ZHONG_YAO\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: yaoId
                "\"ID\" INTEGER NOT NULL ," + // 1: ID
                "\"YAO_LIST\" TEXT," + // 2: yaoList
                "\"NAME\" TEXT," + // 3: name
                "\"HEIGHT\" INTEGER NOT NULL ," + // 4: height
                "\"TEXT\" TEXT," + // 5: text
                "\"SIGNATURE_ID\" INTEGER NOT NULL ," + // 6: signatureId
                "\"SIGNATURE\" TEXT);"); // 7: signature
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ZHONG_YAO\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ZhongYao entity) {
        stmt.clearBindings();
 
        Long yaoId = entity.getYaoId();
        if (yaoId != null) {
            stmt.bindLong(1, yaoId);
        }
        stmt.bindLong(2, entity.getID());
 
        String yaoList = entity.getYaoList();
        if (yaoList != null) {
            stmt.bindString(3, yaoList);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(4, name);
        }
        stmt.bindLong(5, entity.getHeight());
 
        String text = entity.getText();
        if (text != null) {
            stmt.bindString(6, text);
        }
        stmt.bindLong(7, entity.getSignatureId());
 
        String signature = entity.getSignature();
        if (signature != null) {
            stmt.bindString(8, signature);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ZhongYao entity) {
        stmt.clearBindings();
 
        Long yaoId = entity.getYaoId();
        if (yaoId != null) {
            stmt.bindLong(1, yaoId);
        }
        stmt.bindLong(2, entity.getID());
 
        String yaoList = entity.getYaoList();
        if (yaoList != null) {
            stmt.bindString(3, yaoList);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(4, name);
        }
        stmt.bindLong(5, entity.getHeight());
 
        String text = entity.getText();
        if (text != null) {
            stmt.bindString(6, text);
        }
        stmt.bindLong(7, entity.getSignatureId());
 
        String signature = entity.getSignature();
        if (signature != null) {
            stmt.bindString(8, signature);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ZhongYao readEntity(Cursor cursor, int offset) {
        ZhongYao entity = new ZhongYao( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // yaoId
            cursor.getInt(offset + 1), // ID
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // yaoList
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // name
            cursor.getInt(offset + 4), // height
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // text
            cursor.getLong(offset + 6), // signatureId
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7) // signature
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ZhongYao entity, int offset) {
        entity.setYaoId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setID(cursor.getInt(offset + 1));
        entity.setYaoList(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setHeight(cursor.getInt(offset + 4));
        entity.setText(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setSignatureId(cursor.getLong(offset + 6));
        entity.setSignature(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ZhongYao entity, long rowId) {
        entity.setYaoId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ZhongYao entity) {
        if(entity != null) {
            return entity.getYaoId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ZhongYao entity) {
        return entity.getYaoId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
