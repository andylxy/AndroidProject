package run.yigou.gxzy.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import run.yigou.gxzy.greendao.entity.ChatSessionBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CHAT_SESSION_BEAN".
*/
public class ChatSessionBeanDao extends AbstractDao<ChatSessionBean, Long> {

    public static final String TABLENAME = "CHAT_SESSION_BEAN";

    /**
     * Properties of entity ChatSessionBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Title = new Property(1, String.class, "title", false, "TITLE");
        public final static Property Preview = new Property(2, String.class, "preview", false, "PREVIEW");
        public final static Property CreateTime = new Property(3, String.class, "createTime", false, "CREATE_TIME");
        public final static Property UpdateTime = new Property(4, String.class, "updateTime", false, "UPDATE_TIME");
        public final static Property IsDelete = new Property(5, int.class, "isDelete", false, "IS_DELETE");
    }

    private DaoSession daoSession;


    public ChatSessionBeanDao(DaoConfig config) {
        super(config);
    }
    
    public ChatSessionBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CHAT_SESSION_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"TITLE\" TEXT NOT NULL ," + // 1: title
                "\"PREVIEW\" TEXT," + // 2: preview
                "\"CREATE_TIME\" TEXT NOT NULL ," + // 3: createTime
                "\"UPDATE_TIME\" TEXT NOT NULL ," + // 4: updateTime
                "\"IS_DELETE\" INTEGER NOT NULL );"); // 5: isDelete
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CHAT_SESSION_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ChatSessionBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getTitle());
 
        String preview = entity.getPreview();
        if (preview != null) {
            stmt.bindString(3, preview);
        }
        stmt.bindString(4, entity.getCreateTime());
        stmt.bindString(5, entity.getUpdateTime());
        stmt.bindLong(6, entity.getIsDelete());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ChatSessionBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getTitle());
 
        String preview = entity.getPreview();
        if (preview != null) {
            stmt.bindString(3, preview);
        }
        stmt.bindString(4, entity.getCreateTime());
        stmt.bindString(5, entity.getUpdateTime());
        stmt.bindLong(6, entity.getIsDelete());
    }

    @Override
    protected final void attachEntity(ChatSessionBean entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ChatSessionBean readEntity(Cursor cursor, int offset) {
        ChatSessionBean entity = new ChatSessionBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // title
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // preview
            cursor.getString(offset + 3), // createTime
            cursor.getString(offset + 4), // updateTime
            cursor.getInt(offset + 5) // isDelete
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ChatSessionBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTitle(cursor.getString(offset + 1));
        entity.setPreview(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setCreateTime(cursor.getString(offset + 3));
        entity.setUpdateTime(cursor.getString(offset + 4));
        entity.setIsDelete(cursor.getInt(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ChatSessionBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ChatSessionBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ChatSessionBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
