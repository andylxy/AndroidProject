package run.yigou.gxzy.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import run.yigou.gxzy.greendao.entity.ChatSummaryBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CHAT_SUMMARY_BEAN".
*/
public class ChatSummaryBeanDao extends AbstractDao<ChatSummaryBean, Long> {

    public static final String TABLENAME = "CHAT_SUMMARY_BEAN";

    /**
     * Properties of entity ChatSummaryBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property SessionId = new Property(1, Long.class, "sessionId", false, "SESSION_ID");
        public final static Property Title = new Property(2, String.class, "title", false, "TITLE");
        public final static Property Content = new Property(3, String.class, "content", false, "CONTENT");
        public final static Property CreateTime = new Property(4, String.class, "createTime", false, "CREATE_TIME");
        public final static Property IsDelete = new Property(5, int.class, "isDelete", false, "IS_DELETE");
    }


    public ChatSummaryBeanDao(DaoConfig config) {
        super(config);
    }
    
    public ChatSummaryBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CHAT_SUMMARY_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"SESSION_ID\" INTEGER NOT NULL ," + // 1: sessionId
                "\"TITLE\" TEXT," + // 2: title
                "\"CONTENT\" TEXT NOT NULL ," + // 3: content
                "\"CREATE_TIME\" TEXT NOT NULL ," + // 4: createTime
                "\"IS_DELETE\" INTEGER NOT NULL );"); // 5: isDelete
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CHAT_SUMMARY_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ChatSummaryBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getSessionId());
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(3, title);
        }
        stmt.bindString(4, entity.getContent());
        stmt.bindString(5, entity.getCreateTime());
        stmt.bindLong(6, entity.getIsDelete());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ChatSummaryBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getSessionId());
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(3, title);
        }
        stmt.bindString(4, entity.getContent());
        stmt.bindString(5, entity.getCreateTime());
        stmt.bindLong(6, entity.getIsDelete());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ChatSummaryBean readEntity(Cursor cursor, int offset) {
        ChatSummaryBean entity = new ChatSummaryBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // sessionId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // title
            cursor.getString(offset + 3), // content
            cursor.getString(offset + 4), // createTime
            cursor.getInt(offset + 5) // isDelete
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ChatSummaryBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSessionId(cursor.getLong(offset + 1));
        entity.setTitle(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setContent(cursor.getString(offset + 3));
        entity.setCreateTime(cursor.getString(offset + 4));
        entity.setIsDelete(cursor.getInt(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ChatSummaryBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ChatSummaryBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ChatSummaryBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
